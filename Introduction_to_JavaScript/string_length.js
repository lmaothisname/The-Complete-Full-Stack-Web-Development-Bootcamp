// a built-in feature  of JavaScipt strings used to count the exact number of characters inside
// that string

console.log("kiet".length);

var tweet = prompt("Compose your tweet:");
alert("you have written " + tweet.length + "characters, you have " + (280 - tweet.length) + "characters reamaining.");
